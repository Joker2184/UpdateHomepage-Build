name: versions
fill:
  Image_link: ${I_Link}
  version-id: ${file_name}
  title: 📰 ${file_name}
  Updatedate:  ${date}
  UpdateWriter:  ${Writer}
  templates:
    - NewVersion
    - NewsCard
    - Raw

    
