name: versions
fill:
  Image_link: ${I_Link}
  version-id: ${file_name}
  title: 📰 ${file_name}
  Updatedate:  ${date}
  UpdateWriter:  ${Writer}
  VersionType:  ${Type}
  VersionNote:  ${Note}
  templates:
    - NewVersion
    - NewsCard
    - Raw

    
