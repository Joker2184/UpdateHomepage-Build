name: Run Script

on:
  workflow_dispatch:  # 纯手动触发

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - run: python your_script.py  # 替换为你的脚本文件名
