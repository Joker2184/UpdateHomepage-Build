name: Sync PCL Latest Release

on:
  schedule:
    - cron: '0 * * * *'  # 每小时运行一次（UTC 时间）
  workflow_dispatch:      # 允许手动触发

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: 🛎️ Checkout Repository
        uses: actions/checkout@v4
        with:
          repository: Joker2184/UpdateHomepage-Build
          token: ${{ secrets.PAT_TOKEN }}
          fetch-depth: 1

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 Install Dependencies
        run: pip install requests

      - name: ▶️ Run Release Fetcher
        run: python Actions/get_latest_release.py

      - name: 💾 Commit and Push (if new file)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .
          if ! git diff --staged --quiet; then
            git commit -m "feat(snapshot): add new PCL release changelog"
            git push
          else
            echo "⏭️ 无变更，跳过推送"
          fi
